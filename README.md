# CodeSnips

Small collection of HTML/CSS cheat sheets and examples for quick reference.

To view locally, open `index.html` in your browser or serve the folder with a simple static server.

Example:

```bash
# macOS / Linux
python3 -m http.server 8000
# then open http://localhost:8000
```
